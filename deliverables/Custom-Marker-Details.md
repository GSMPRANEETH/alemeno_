# Custom Marker Details

As per the assignment requirements, the solution targets one of the supplied markers (`Marker1`) rather than introducing a newly generated custom marker.

Because `Marker1` was selected from the provided assets, there is no custom generation logic or new measurements to supply.

For testing orientation, please refer to the `fixtures/marker1` directory which contains sample images in various orientations used to validate the approach.
